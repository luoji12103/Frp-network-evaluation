services:
  panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mc-netprobe-panel
    restart: unless-stopped
    ports:
      - "${MC_NETPROBE_WEBUI_PORT:-8765}:8765"
    environment:
      MC_NETPROBE_WEBUI_HOST: "0.0.0.0"
      MC_NETPROBE_WEBUI_PORT: "8765"
      MC_NETPROBE_PANEL_DB_PATH: "data/monitor.db"
      TZ: "${TZ:-UTC}"
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./data:/app/data
      - ./config/agent:/app/config/agent
      - ./results:/app/results
      - ./logs:/app/logs
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8765/api/v1/dashboard', timeout=5).read()"
      interval: 20s
      timeout: 5s
      retries: 3
      start_period: 15s
