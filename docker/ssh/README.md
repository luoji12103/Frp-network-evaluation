Put SSH private keys and SSH config files for remote node access in this directory
if you do not want to mount your normal `~/.ssh` directory.

Typical files:

- `id_ed25519`
- `id_rsa`
- `config`
- `known_hosts`

This directory is ignored by Git except for this README.
